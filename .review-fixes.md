# Review fix-up plan

Consolidated from 7 reviewer agents over the last 12 commits. Priority order;
each tier should typically be one commit (bundled when changes are tightly
coupled). Code review after each commit.

---

## Tier 1 — critical correctness + leaks

Bundle into one commit: all three share the lifecycle theme.

- [ ] **T1. Port-bind NPE.** `AskUserMcpServer.start` constructs the
      `stopFn` closure with `() => binding.stop()` — if
      `NettySyncServer().start()` throws, `binding` is never assigned;
      `mcpServer.close()` later NPEs. Move binding inside try, or build
      the closure after start.

- [ ] **T2. Drainer thread + bridge channel leak.** `ClaudeConversation.onFinalize`
      stops the MCP server but doesn't close the bridge. The daemon
      drainer parks on `bridge.take()` forever — until flow scope ends.
      Long flows accumulate one parked thread + closed-over conversation
      graph per interactive call. Add `AskUserBridge.close()`, call from
      `onFinalize` before `mcpServer.close()`. Also: track in-flight
      reply channels, signal them on close so the Netty worker returns.

- [ ] **T3. Netty worker stranded mid-`ask`.** Same fix as T2 — closing
      the bridge errors any blocked `reply.receive()`. After T2's
      `bridge.close()`, the handler unwinds with a tool error, the agent
      sees a clean failure rather than the binding-stop force-killing
      the worker.

## Tier 2 — important

Each its own commit.

- [ ] **T4. Enforce `canAskUser` at compile time.** Today
      `Plan.interactive.from(prompt, codex)` compiles and silently
      degrades. Add a typeclass `CanAskUser[B <: BackendTag]` with an
      instance for `ClaudeCode` only; constrain
      `Plan.interactive.from`'s `B` to require it. Keep the runtime
      `canAskUser` flag for programmatic queries.

- [ ] **T5. `TerminalEventListener` lock + mailbox-full stall.** The
      lock is held across `output.log(...)` / `output.setStatus(...)`
      tells, which can block if the actor mailbox is full. Concurrent
      `currentIndent()` reads from the body thread then block on a
      listener stuck on a mailbox. Two-phase: mutate stage state + format
      under lock, release lock, then dispatch.

- [ ] **T6. Bounded `StreamConversation.eventQueue`.** No cap +
      non-blocking `offer` = unbounded growth if consumer is slow.
      Switch to a bounded queue with blocking `put`; backpressure flows
      naturally to the subprocess via pipe buffers.

- [ ] **T7. Delete `.orca-mcp-<port>.json` on conversation end.**
      Register an `AutoCloseable` in `sessionResources` that
      `os.remove`s the config file. Long flows currently accumulate
      orphans.

## Tier 3 — nice-to-have

Group these into 2-3 small commits by theme.

- [ ] **T8. `AskUserBridge.respond` idempotent.** `AtomicBoolean` guard
      so a double-`respond` returns silently instead of hanging the
      second sender.

- [ ] **T9. Rename `take()` → `nextQuestion()` returning a named
      `PendingQuestion(question: String, respond: String => Unit)`.**
      Closure semantics become visible at call site.

- [ ] **T10. Rename `LlmConfig.withAlsoAllowedTool` →
      `autoApproveAlso(tool)`.** Reads cleaner; "also" no longer looks
      like a typo.

- [ ] **T11. Single source for the `"ask_user"` slug.** Today
      `AskUserMcpServer.start` and `ClaudeBackend.AskUserToolName` each
      build the name independently; a rename would drift silently. Pull
      `AskUserMcpServer.ToolSlug` constant referenced from both.

- [ ] **T12. Replace bare `catch _: Throwable` with `NonFatal`.** Three
      sites: `TerminalOutput.scala:91`, `ClaudeConversation.scala:108`
      and `:129`. `NonFatal` lets `InterruptedException` propagate.

- [ ] **T13. Bound `suspendedBuffer`.** Cap at e.g. 256 entries; drop
      older or coalesce when over.

- [ ] **T14. Animator: don't block on a full mailbox.** Either raise
      the actor's `BufferCapacity`, or skip the tick if the mailbox
      can't accept it.

- [ ] **T15. Missing tests.**
      - `ask_user` ToolCall suppression in `handleAssistantTurn`.
      - `canAskUser == false` for default `ClaudeConversation`
        construction.
      - Bridge scope-tear-down: forked `ask`, leave scope, fork exits
        cleanly.

## Tier 4 — cosmetic / style

One omnibus commit if they accumulate cleanly.

- [ ] **T16. Suppress `ToolResult` echo for `ask_user`.** Track
      outstanding `tool_use_id`s suppressed by `handleAssistantTurn`;
      skip matching results in `handleUserTurn`.

- [ ] **T17. Drainer fork instead of raw `Thread`.** Depends on T2's
      lifecycle work; if the conversation gains an Ox scope, switch
      `new Thread(...).setDaemon(true).start()` to `forkDiscard`.

- [ ] **T18. Plain `var`s on single-threaded paths.**
      `ClaudeConversation.sessionIdRef`, `initModelRef`,
      `deltasSinceTurnBoundary` — atomic-typed but only ever touched by
      the reader thread.

- [ ] **T19. Drop duplicate tests in `ConversationRendererTest`.**
      Three overlapping "prose flushes on TurnEnd" cases. Keep one,
      merge or drop the others. Fix the misleading name on
      "AssistantTextDelta streams the text inline without a trailing
      newline".

- [ ] **T20. `AskUserMcpServer.boundPort` cleanup.** Exposed only so
      the caller can name the config file. Move the filename concern
      into the server, or rename `boundPort` → `port` and add a code
      comment near the caller.

- [ ] **T21. `currentIndent()` lock contention.** `@volatile var
      indent: String` updated by listener under its lock, read lock-free
      from the renderer hot path.

---

## Progress

Tier 1: not started
Tier 2: not started
Tier 3: not started
Tier 4: not started
