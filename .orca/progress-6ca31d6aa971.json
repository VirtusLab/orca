{"header":{"startingBranch":"drop-confidence-machinery","branch":"single-pass-review-final-loop","promptHash":"6ca31d6aa971","branchMode":{"type":"Created"},"userPrompt":"Implement \"PR 1 — Review shape: single-pass per task, capped whole-run final loop\" from the review-rework plan, exactly as specified below. Scope is ONLY PR 1 — do not implement PRs 3 or 4. PR 2 (confidence-gate removal) is ALREADY IMPLEMENTED on the branch you are starting from, including ADR 0022 (adr/0022) which records this PR's decisions — amend the ADR only if implementation forces a deviation. Note the plan's file/line references were verified against master BEFORE the gate removal landed; the gate machinery (confidenceGate, GateLedger, applyGate, LoopExit/conclude) is already gone and the loop is simpler than the plan's snapshot — adapt locations accordingly, the behavioral requirements are unchanged. Follow the repo's conventions (AGENTS.md): zero compiler warnings, scalafmt, targeted non-duplicative tests, concise present-tense comments.\n\n===== SPECIFICATION (context; implement only the PR 1 part) =====\n# Review rework — specification\n\nA post-mortem of an `implement.sc` run showed the review loop missing real\ndefects: findings raised by reviewers were silently dropped by the confidence\ngate, reviewers deferred to the plan, cross-task interactions were never\nreviewed, and relevant reviewers were never selected. This specification\ndescribes the functional changes, split into four PRs. Landing order:\nPR 2 → PR 1 → PR 3 → PR 4. One combined ADR records all four.\n\n## PR 2 — Drop the confidence machinery\n\nReviewers no longer attach confidence scores, and no gate filters their\nfindings. A reviewer reports a finding only when it believes the finding\nshould be fixed; every reported finding reaches the fixer. The fixer may\ndecline a finding with a reason; declines are shown to reviewers in later\nrounds (who may re-report with a rebuttal) and reported at the end of the\nrun. Nothing is ever silently dropped.\n\nSeverity labels stay (they order and label reports). Reviewer narrowing\nacross rounds now keys on what a reviewer actually reported, so a reviewer\nis retired only on genuine silence — never because its findings were filtered.\n\nNo damping rule for repeated declines: the round cap and the\nnothing-was-fixed halt already bound re-litigation. Revisit if real runs\nshow token waste on stubborn disagreements.\n\n## PR 1 — Review shape: single pass per task, whole-branch final review\n\nPer-task review stops being a loop. A new helper, `reviewThenFix`, runs the\npicked reviewers once, hands the findings to the fixer once, and returns —\nno re-review. The fixer's \"fixed\" claims are trusted unverified at task\nlevel.\n\nVerification moves to a new final stage: after all tasks, the flow reviews\nthe **run's entire change set** (everything changed since the run started,\ncommitted or not) with the existing multi-round review-and-fix loop, capped\nat 5 rounds. This is where\ncross-task and out-of-diff interactions get fresh-eyes review.\n\nAdopted by the five task-based flows (`implement`, `implement-interactive`,\n`implement-enhanced`, `issue-pr`, `issue-pr-bugfix`); flows that open a PR\nrun the final review before opening it. `simple.sc` (its one stage already\nspans the whole branch) and `review.sc` (no fixer) are unchanged.\n\nThe diff base is the run's starting commit, recorded in the progress log at\nsetup. This works identically with and without `--skip-branch`, needs no\nremote, and stays stable across resumes.\n\n## PR 3 — Anti-deference reviewer prompts\n\nThree prompt changes:\n\n1. The instructions sent to every reviewer gain three rules: a deliberate\n   or planned decision is evidence of intent, not of correctness — never a\n   reason to withhold or soften a finding; findings whose consequence is\n   user data loss, silent inversion of user intent, or a blocked/hung\n   process must always be reported, at the severity the consequence\n   deserves, even when the plan explicitly chose the behavior; the cost of\n   the fix never lowers a finding's severity.\n2. The fixer prompt requires stating which option was taken when a\n   suggestion offered alternatives (\"do X, or document why Y is safe\"); the\n   re-review prompt requires verifying that the chosen option resolves the\n   original concern, not merely that it was performed.\n3. The declined-findings block shown to reviewers states that a decline\n   justified only by \"the plan chose this\" is insufficient for the\n   mandatory-report categories above — such findings are to be re-reported.\n\n## PR 4 — Reviewer-picker inclusion checklists\n\nEach built-in reviewer's description gains a short, researched \"include\nwhen\" checklist of concrete diff signals (e.g. performance: spawns\nsubprocesses, performs IO, adds work inside loops), so the picker matches\nchecklists against the changed files instead of judging scope relevance.\nThe picker must also state a brief rationale for the reviewers it excludes,\nmaking selection auditable. The checklists themselves are the deliverable:\n2–4 clauses per reviewer, each checkable against a diff.\n\n## Out of scope (tracked separately)\n\nA guaranteed selection floor (e.g. full roster in the final review's first\nround); severity calibration; verifying fixer \"fixed\" claims at task level;\nsurfacing end-of-run open findings in the PR body.\n\n===== IMPLEMENTATION PLAN — PR 1 (the work to do) =====\n# PR 1 — Review shape: single-pass per task, capped whole-run final loop\n\n## Public API\n\n**New helper — `reviewThenFix`** (in `ReviewLoop.scala`, exported from\n`runner/src/main/scala/orca/exports.scala`):\n\n```scala\ndef reviewThenFix[B <: BackendTag](\n    coderSession: FlowSession[B],\n    reviewers: List[Agent[?]],\n    task: Task,\n    userRequest: Option[String] = None,\n    formatCommands: Configured[List[String]] = Configured.FromSettings,\n    lint: Configured[Lint] = Configured.FromSettings\n)(using FlowContext, InStage, FlowControl, WorkspaceWrite): IgnoredIssues\n```\n\nDeliberately narrower than `reviewAndFixLoop`: no `maxIterations` (no loop),\nno `reviewerSelection`/`fixInstructions`/`diff` — the flows pass none of\nthem, selection is `agentDriven` (narrowing is a cross-round concept), and\nthe diff is the stage sample. Consumers are version-pinned scripts, so a\nparameter is added when a caller needs it, not before.\n\nSemantics: format workspace → run selection once → fan out the selected\nreviewers + lint once (same `CheckedPar` fan-out, same Steps) → if findings,\none fix turn through the coder session → reconcile → announce → return. The\nfixer's `fixed` claims are trusted unverified — per-task verification is\ndelegated to the final whole-run loop, which sees the same code with\nfresh eyes instead of a convergence-primed session. `ignored` +\n`unaccounted` come back in `IgnoredIssues`.\n\nImplementation shape: refactor `ReviewFixLoop` so one round (evaluate =\nformat+select+fan-out; fixTurn = fix+reconcile+announce) is a named\nprimitive, with `run()` looping it and a new `runOnce()` calling it once —\nthis also discharges the dual-skeleton drift noted in\n`docs/research/2026-08-review/review-loop/06-dual-loop-skeleton-drift.md`\nrather than adding a third skeleton.\n\n## Whole-run diff source\n\nThe base is the run's **starting commit, recorded in the progress header at\nsetup**. On a fresh run (both modes), `FlowLifecycle` captures\n`git.headCommit()` at the branch-binding point — before orca's own\nstack-settings/header commits — and writes it into the `ProgressHeader` as a\nnew `startingCommit` field; a resumed run reads it back from the header, so\nthe base is stable across sessions regardless of how the starting branch has\nmoved since. This needs no remote, works identically with `--skip-branch`,\nand replaces any merge-base computation.\n\nThe header is untrusted input (hand-editable, committed): on load, validate\n`startingCommit` as a hex string (arg-injection safe) — it is only ever used\nas a read-only diff base, so no stronger check is needed. A header lacking\nthe field (hand-edited; old logs are a non-concern, consumers are\nversion-pinned) skips the final review with a warning Step.\n\nExisting pieces (verified): `GitTool.reviewChanges(since)` gives diff-text +\nfile list of *working tree vs any rev*, untracked included, re-sampled per\ncall — exactly what the loop's `ReviewDiffSource.Sampled` consumes. No\n`GitTool` changes are needed. Missing piece: the recorded commit reaches\nneither `FlowContext` nor the loop — `FlowSetup` (constructed in\n`FlowLifecycle.setup`, in scope at `DefaultFlowContext` construction,\n`runner/src/main/scala/orca/flow.scala:448`) carries it there. Additions:\n\n```scala\n// ProgressHeader: startingCommit: String (fresh runs write it; validated on load)\n\n// FlowContext: private[orca] def startingCommit: String\n//   (consumed only by the loop's WholeRun resolution — not public script API)\n\n// ReviewDiff (flow/src/main/scala/orca/review/ReviewDiffSource.scala)\nenum ReviewDiff:\n  case SampleFromStage\n  case Pinned(diff: String)\n  case WholeRun\n```\n\n`WholeRun` resolves at loop entry (the existing capabilities-at-entry\npattern; the selector's CC `->` arrow is untouched) to a diff source\nsampling `git.reviewChanges(Some(ctx.startingCommit))`, its own `diffIntro`,\nthe base exposed for the reviewers' `baseNote`, and\n`selectorFiles = git.changedFiles(Some(ctx.startingCommit))`. Bounding via\n`BoundedDiff.reviewPayload` comes free through the `Sampled` path. Whether\nthis is a new `ReviewDiffSource.WholeRun` or a parameterised `Sampled` is\nimplementer's choice.\n\nThe final review itself is just\n`reviewAndFixLoop(..., diff = ReviewDiff.WholeRun, maxIterations = 5)`\ninside its own `stage(\"Final review\")` — no new loop machinery, no new\npublic constant (the flows write the literal `5`; the flow-pin test\nhardcodes it, see below).\n\n## Per-flow adoption (`flows/`)\n\n| Flow | Change |\n|---|---|\n| `implement.sc` | Per task: `reviewThenFix(...)`. After the task loop: `stage(\"Final review\"): reviewAndFixLoop(coderSession = session, reviewers = allReviewers(reviewAgent), task = Task(Title(<final-review title>), plan.brief), diff = ReviewDiff.WholeRun, maxIterations = 5)`. |\n| `implement-interactive.sc` | Same as `implement.sc`. |\n| `implement-enhanced.sc` | Same; final review placed **after** \"Update documentation\" and before `openPrFromBranch`, so the PR carries the final fixes and the reviewers see the docs too. |\n| `issue-pr.sc` | Per task `reviewThenFix(..., userRequest = Some(issuePayload))`; final review stage (same `userRequest`) before `openPrFromBranch`. |\n| `issue-pr-bugfix.sc` | Per fix-task `reviewThenFix(..., userRequest = Some(issuePayload))` inside `planAndImplementFix`; final review stage after `planAndImplementFix`, before \"Push fix + finalise PR\". Fine that the branch carries the failing test — by final-review time the fix tasks are done. |\n| `simple.sc` | No change (its one stage already spans the whole branch; the loop plays both roles). |\n| `review.sc` | No change — no fixer, no loop; already a single pass. |\n\nAll five adopting flows change in this PR — adoption is coupled, not\noptional: a flow with single-pass `reviewThenFix` but no final loop would\nship fixer-trusted-unverified fixes.\n\n`shell/src/test/scala/orca/shell/flows/BuiltInFlowsTest.scala:36` (\"every\nfix-loop call in a flow states the library's default cap\") is reworked:\n`reviewThenFix` calls carry no cap; the final-review `reviewAndFixLoop`\ncalls must state `maxIterations = 5` (hardcoded in the test with a comment —\nno public constant; `DefaultMaxIterations` keeps its existing pin role).\n\n## File-by-file changes\n\n- `flow/src/main/scala/orca/review/ReviewLoop.scala` — `runOnce()` +\n  round-primitive refactor, `reviewThenFix`, `WholeRun` resolution at\n  `reviewAndFixLoop` entry.\n- `flow/src/main/scala/orca/review/ReviewDiffSource.scala` —\n  `ReviewDiff.WholeRun` + its source.\n- Progress header: `ProgressHeader` (`flow/src/main/scala/orca/progress/`)\n  gains `startingCommit`; `FlowLifecycle` writes it on a fresh binding\n  (`git.headCommit()` at the binding point) and reads it back on resume;\n  header validation covers the new field (hex check). Header\n  serialization/golden tests updated.\n- `flow/src/main/scala/orca/FlowContext.scala`,\n  `runner/src/main/scala/orca/runner/DefaultFlowContext.scala`,\n  `runner/src/main/scala/orca/flow.scala` — thread `startingCommit`\n  (`private[orca]`, from `FlowSetup`);\n  `flow/src/test/scala/orca/TestFlowContext.scala` gains it.\n- `runner/src/main/scala/orca/exports.scala` — export `reviewThenFix`.\n- Five `flows/*.sc` per the table.\n- Tests: `reviewThenFix` scenarios (findings→one fix turn→no re-evaluation;\n  clean review→no fix turn; declines and unaccounted land in the result;\n  selection runs once); `WholeRun` tests (base = recorded commit,\n  re-sampling across rounds picks up fix edits, resume reads the header's\n  commit, missing/invalid `startingCommit` skips with a warning);\n  `BuiltInFlowsTest` rework.\n- Docs: README quick-start + Review utilities table (`reviewThenFix` row,\n  `WholeRun`, final-review pattern). ADR 0022 already records this PR\n  (amend only on deviation).\n\n## Test plan\n\nUnit tests as above (in-memory `FakeAgent`/`TestFlowControl` per existing\nfixture; `orca.testkit.GitRepo` for git-level tests). One live sanity run of\n`flows/implement.sc` on the seeded calculator example\n(`examples/runnable/01-simple`) before merge — the flow-shape change is\nexactly the kind of thing unit tests under-cover; `issue-pr-bugfix.sc` has\nthe one non-trivial final-review placement if a second spot-check is wanted.\n\n## Acceptance criteria\n\n- `reviewThenFix` performs exactly one evaluation and at most one fix turn;\n  every reported finding is fixed or returned in `IgnoredIssues` — never\n  silently dropped.\n- The final loop reviews the starting-commit-to-working-tree change set,\n  re-sampled per round, capped at 5; a header without a valid\n  `startingCommit` skips the final review with a warning.\n- Each shipped flow compiles (scala-cli) and follows the table;\n  `BuiltInFlowsTest` pins the caps.\n- ADR 0022 still accurate; zero warnings; scalafmt.\n\n**Size: L** (loop refactor + context/git plumbing + five flows + tests).\n\n---","flowName":"implement.sc"},"entries":[{"id":"Plan#0","name":"Plan","resultJson":{"epicId":"single-pass-review-final-loop","description":"Reshape orca's review machinery (PR 1 of the review rework, ADR 0022 §2): per-task review stops being a multi-round loop and becomes a single pass — a new public helper `reviewThenFix` runs the picked reviewers once, hands findings to the fixer once, and returns, trusting the fixer's \"fixed\" claims at task level. Verification moves to a new end-of-run stage: the existing multi-round `reviewAndFixLoop`, capped at 5 rounds, reviews the run's entire change set (working tree vs the commit the run started at), which is where cross-task and out-of-diff interactions get fresh-eyes review.\n\nThe whole-run diff base is the run's starting commit, captured by `FlowLifecycle` at the branch-binding point on a fresh run and persisted in the progress header (`startingCommit`), read back (hex-validated, untrusted input) on resume, and threaded through `FlowSetup` into `FlowContext` so a new `ReviewDiff.WholeRun` case can resolve it at loop entry. A header without a valid `startingCommit` skips the final review with a warning Step — never aborts, never silently reviews the wrong base. All five task-based flows (`implement`, `implement-interactive`, `implement-enhanced`, `issue-pr`, `issue-pr-bugfix`) adopt both halves together — adoption is coupled, since single-pass review without the final loop would ship unverified fixes. `simple.sc` and `review.sc` are unchanged. PR 2 (confidence-gate removal) already landed on this branch; ADR 0022 records this PR's decisions and is amended only if implementation forces a deviation.","tasks":[{"title":"Refactor ReviewFixLoop into named round primitives (behavior-preserving)","description":"In `flow/src/main/scala/orca/review/ReviewLoop.scala`, extract the loop body of `ReviewFixLoop.run()` into named primitives so a single round is callable on its own: an evaluate step (format workspace + apply the prepared selection + reviewer/lint fan-out — essentially the existing `evaluate`) and a fix-turn step (fix + `FixOutcome.reconcile` + `announceFixTurn`, returning the reconciled outcome). `run()` keeps looping these primitives with `stopPolicy`/`carryPastFixes` exactly as today. No public API or behavior change: all existing tests (`ReviewAndFixTest`, `ReviewFixFlowTest`, `FixLoopTest`, `CcNegativeCompileTest`) stay green unmodified. Preserve the CC constraints: `fc`/`ws` remain method parameters, the `CheckedPar` fan-out and the CC-forced `.++[() => AgentOutcome]` widening stay intact, `ctx`/`ev` stay explicitly passed where they are today. This discharges the dual-skeleton drift noted in `docs/research/2026-08-review/review-loop/06-dual-loop-skeleton-drift.md` ahead of adding the single-pass entry point."},{"title":"Add reviewThenFix (single-pass per-task review) and export it","description":"Add `ReviewFixLoop.runOnce()` calling the round primitives once: evaluate (selection prepared with `ReviewerSelector.agentDriven`, run once) → if findings, one fix turn → fold the outcome's `ignored` + `unaccounted` (with a reason) into the returned `IgnoredIssues` via `recordIgnored` → `announceExit` → return. No re-evaluation ever; `fixed` claims trusted unverified. Add the public `def reviewThenFix[B <: BackendTag](coderSession, reviewers, task, userRequest = None, formatCommands = Configured.FromSettings, lint = Configured.FromSettings)(using FlowContext, InStage, FlowControl, WorkspaceWrite): IgnoredIssues` wrapper (resolves `Configured` at entry like `reviewAndFixLoop`, diff source = stage sample) and export it from `runner/src/main/scala/orca/exports.scala`. Tests (using `ReviewLoopFixture`): findings → exactly one fix turn and no second evaluation (FakeAgent throws past its scripted outputs); clean review → no fix turn; declines and unaccounted titles land in the returned `IgnoredIssues` with reasons; selection runs exactly once."},{"title":"Record the run's starting commit in the progress header and thread it to FlowContext","description":"Add `startingCommit: Option[String] = None` to `ProgressHeader` (same sanctioned tolerant-decoding shape as `userPrompt`/`flowName`). In `FlowLifecycle.setup`, capture `git.headCommit()` at the branch-binding point (post-stash, before any settings/header commit); the fresh arm (`freshRun`) writes it into the header as `Some`, the resume arm (`resumeBinding`) reads it back with a lenient hex-string check — missing or non-hex yields `None`, never an abort (it is only ever a read-only diff base). Thread it via `BranchBinding`/`FlowSetup` into `FlowContext` as `private[orca] def startingCommit: Option[String]`, implemented by `DefaultFlowContext` (constructor fact wired in `runner/src/main/scala/orca/flow.scala` alongside `stackSettings`) and by `TestFlowContext`/`TestFlowControl` (seed from the temp repo's head in `TestFlowControl.create`). Tests: fresh run records HEAD-at-binding in the header (both normal and skip-branch modes, in `FlowLifecycleTest` style over `GitRepo`); resume reads the recorded value back even after the branch gained commits; missing field and non-hex value both resolve to `None`; header serialization round-trip covers the new field."},{"title":"Add ReviewDiff.WholeRun with skip-and-warn on a missing starting commit","description":"Add `case WholeRun` to the public `ReviewDiff` enum (`flow/src/main/scala/orca/review/ReviewDiffSource.scala`). Resolve it at `reviewAndFixLoop` entry (the existing capabilities-at-entry pattern; the selector's CC `->` arrow untouched): with `ctx.startingCommit = Some(c)`, build a diff source sampling `git.reviewChanges(Some(c))` with `selectorFiles = git.changedFiles(Some(c))`, `base = Some(c)`, and a WholeRun-specific `diffIntro` stating the change set covers everything since the run started (whether this is a new `ReviewDiffSource` case or a parameterised `Sampled` is implementer's choice; bounding via `BoundedDiff.reviewPayload` comes free through the Sampled path). With `ctx.startingCommit = None`, emit a warning `OrcaEvent.Step` that names why the final review is skipped and return `IgnoredIssues(Nil)` without constructing the loop. Tests (over `GitRepo` + `ReviewLoopFixture.control`): the sampled diff is based on the recorded commit, not the stage base; re-sampling across rounds picks up fixer edits; `startingCommit = None` produces the warning Step and runs no reviewers."},{"title":"Adopt the new shape in the five flows and rework BuiltInFlowsTest","description":"Update the five task-based flows in `flows/`: per task, replace the `reviewAndFixLoop(..., maxIterations = 3)` call with `reviewThenFix(...)` (keeping `userRequest = Some(issuePayload)` in `issue-pr.sc` and `issue-pr-bugfix.sc`); after the tasks, add `stage(\"Final review\"): reviewAndFixLoop(coderSession = session, reviewers = allReviewers(reviewAgent), task = ..., diff = ReviewDiff.WholeRun, maxIterations = 5)` — after the task loop in `implement.sc`/`implement-interactive.sc` (task from `plan.brief`), after \"Update documentation\" and before `openPrFromBranch` in `implement-enhanced.sc`, before `openPrFromBranch` in `issue-pr.sc` (same `userRequest`), and after `planAndImplementFix` / before \"Push fix + finalise PR\" in `issue-pr-bugfix.sc` (same `userRequest`; build the final-review Task from the issue payload since `fixPlan` is helper-local, or place the stage at the helper's end). `simple.sc` and `review.sc` unchanged; update each changed flow's header doc-comment to the new shape. Rework `shell/src/test/scala/orca/shell/flows/BuiltInFlowsTest.scala:36`: `reviewThenFix` calls carry no cap, final-review `reviewAndFixLoop` calls must state `maxIterations = 5` (hardcoded in the test with a comment — no public constant), `simple.sc`'s loop still pins `DefaultMaxIterations`. Verify each changed flow compiles with scala-cli (via the publishLocal + dev-pin-rewrite recipe in CONTRIBUTING.md)."},{"title":"Update README, verify ADR 0022 accuracy, and run the live sanity check","description":"README: update the quick-start example to mirror the new `implement.sc` shape (per-task `reviewThenFix` + final `reviewAndFixLoop` with `ReviewDiff.WholeRun`, `maxIterations = 5`); add a `reviewThenFix` row to the \"Review utilities\" table, document `ReviewDiff.WholeRun` where `ReviewDiff` is described, and show the final-review pattern — keep the reference tables complete, prose maximally short. Re-read `adr/0022-review-rework.md` §2 against the implementation and amend only on genuine deviation. Run the live sanity check: `flows/implement.sc` on the seeded calculator example (`examples/runnable/01-simple/create-test-project.sh`), confirming per-task single-pass review and the final whole-run stage both execute; clean `/tmp/orca-*` first if commands start failing with exit 1 (tmpfs exhaustion). Finish with the full-repo gate: `sbt test`, zero warnings, scalafmt clean."}],"brief":"## Codebase briefing — read before implementing any task\n\nPer CLAUDE.md, invoke the `direct-style-scala` skill before writing/reviewing any Scala here. Build/test commands are in CONTRIBUTING.md; use plain `sbt` (not `sbt --client`). Zero compiler warnings, scalafmt (`flows/*.sc` are OUTSIDE scalafmt's scope). Conventions (AGENTS.md): braceless syntax, explicit return types on public members, no default values on domain/persisted fields (one sanctioned exception below), comments state present-tense facts only (never change history), tests target exactly one scenario, named args for same-typed adjacent params.\n\n### Modules\n- `flow/` — review loop (`orca.review`), progress log (`orca.progress`), `FlowContext`/`FlowControl`, `stage`. Tests in `flow/src/test/scala/orca/`.\n- `runner/` — `flow()` entry (`runner/src/main/scala/orca/flow.scala`), `FlowLifecycle`, `DefaultFlowContext`, `exports.scala` (public re-export surface).\n- `shell/` — packages `flows/*.sc` as resources; `shell/src/test/scala/orca/shell/flows/BuiltInFlowsTest.scala` pins them.\n- `flows/*.sc` — the shipped scripts; version-pinned consumers (`//> using dep \"org.virtuslab::orca:0.1.4\"`; the shell's dev-version extraction rewrites the pin, so local testing works via `sbt publishLocal`).\n\n### ReviewLoop.scala (`flow/src/main/scala/orca/review/ReviewLoop.scala`)\nCompiled under `captureChecking`/`separationChecking` (file-header imports). Consequences you must preserve: `fc: FlowControl`/`ws: WorkspaceWrite` are METHOD parameters on `ReviewFixLoop`, never fields; the reviewer fan-out goes through `CheckedPar.mapParUnordered` and the explicit `.++[() => AgentOutcome]` type application is CC-forced (deleting it breaks the compile); the selector's `List[ReviewBatch] -> List[RosterEntry]` uses the pure-function `->` arrow; tapir-`derives` types cannot live in this file (see FixRequest.scala). `orca.CcNegativeCompileTest` pins the fan-out enforcement.\n\nCurrent shape (post PR 2, simpler than the spec's snapshot — no gate machinery exists): `reviewAndFixLoop[B <: BackendTag](coderSession: FlowSession[B], reviewers: List[Agent[?]], task: Task, userRequest: Option[String] = None, reviewerSelection: ReviewerSelector = ReviewerSelector.default, formatCommands: Configured[List[String]] = Configured.FromSettings, lint: Configured[Lint] = Configured.FromSettings, maxIterations: Int = DefaultMaxIterations, fixInstructions: String = ReviewLoopPrompts.Fix, diff: ReviewDiff = ReviewDiff.SampleFromStage)(using ctx: FlowContext, ev: InStage, fc: FlowControl, ws: WorkspaceWrite): IgnoredIssues`. It resolves `Configured` values and the `ReviewDiffSource` at entry (reads `fc.stageBaseCommit` there so the loop carries plain data), then constructs `private[review] class ReviewFixLoop(config: ReviewLoopConfig)(using ctx, ev)` and calls `run()(using fc, ws)`. Inside: `evaluate(state, selectRound, declined)` = `formatWorkspace()` + selector narrowing + `runReviewersAndLint` (the CheckedPar fan-out, per-agent `OrcaEvent.Step`s); `fix(issues)` = `coderSession.resultAs[FixOutcome].run(FixRequest(...))`; `run()` does two-phase selection (`reviewerSelection.prepare(roster, task.title, diffSource.selectorFiles)` ONCE at loop start — the agentDriven picker's LLM call happens here) then a tailrec loop applying `stopPolicy` per round. Shared helpers to reuse in the single-pass path: `stopPolicy`, `recordIgnored` (title-keyed merge), `carryPastFixes`, `announceExit`, `announceFixTurn`, `fixerHaltAdditions` (`NoFixesReason`), `indexSeverities`, `cleanExitMessage`/`FixerHaltMessage`. `ctx`/`ev` are passed EXPLICITLY at several call sites (constructor, announce calls, `prepare`) because the more-specific `fc: FlowControl` would otherwise win implicit search — keep that pattern. `DefaultMaxIterations: Int = 3` stays and keeps its flow-pin role. `ReviewerSelector.agentDriven` (parameterless, resolves `ctx.reviewAgent.cheap`) is the single-round selector for `reviewThenFix`; `ReviewerSelector.default` = agentDriven + narrowing across rounds.\n\n### ReviewDiffSource.scala (`flow/src/main/scala/orca/review/ReviewDiffSource.scala`)\n`enum ReviewDiff { SampleFromStage, Pinned(diff) }` (public) resolves to `private[review] sealed trait ReviewDiffSource` with `sample(): DiffSample`, `base: Option[String]`, `diffIntro: String`, `selectorFiles: List[String]`. `Sampled(git: GitTool, base: Option[String])` already does exactly what WholeRun needs mechanically: `git.reviewChanges(base)` (working tree vs any rev, untracked included, re-sampled per call) bounded via `BoundedDiff.reviewPayload`, `selectorFiles = git.changedFiles(base)`. Only its `diffIntro` wording (\"everything this task has changed since its stage began\") is per-stage-specific — WholeRun needs its own intro stating the change set covers everything since the run started. New-case-vs-parameterised-Sampled is implementer's choice. `GitTool` needs NO changes: `headCommit(): Option[String]` (full hash), `reviewChanges(since: Option[String]): ReviewSample`, `changedFiles(since: Option[String]): List[String]` all exist (`tools/src/main/scala/orca/tools/GitTool.scala`).\n\n### Progress header + lifecycle\n`ProgressHeader` (`flow/src/main/scala/orca/progress/ProgressLog.scala`): `startingBranch, branch, promptHash, branchMode, userPrompt: Option[String] = None, flowName: Option[String] = None`. The `Option = None` shape on the last two is the SANCTIONED exception to the no-defaults rule (ProgressLog tolerant decoding, documented at the codec) — `startingCommit: Option[String] = None` follows that precedent, so the many existing `ProgressHeader(...)` construction sites in tests keep compiling; fresh runs always write `Some`. The header is untrusted on load: `RecoveryCheck.validateHeader` (`flow/src/main/scala/orca/progress/RecoveryCheck.scala`) hard-aborts on bad branch/hash — but `startingCommit` must be LENIENT (missing OR non-hex ⇒ treated as absent; the final review later skips with a warning Step, tests pin \"missing/invalid skips with a warning\"). Validate as a hex string only — it's only ever a read-only diff base (arg-injection safety).\n\n`FlowLifecycle` (`runner/src/main/scala/orca/runner/FlowLifecycle.scala`): `setup(...)` runs cleanliness policy, `resolveStackSettings`, then `session.bindBranch(startBranch, protectedBranches, discovered)` which routes fresh (`freshBinding` → `freshRun`, which creates the branch, optionally commits discovered settings, then `store.writeHeader(ProgressHeader(...))` + `git.forceCommitOnly`) vs resume (`resumeBinding`, which validates the header). Capture `git.headCommit()` at the binding point — after the cleanliness stash, BEFORE `freshRun`'s settings/header commits — and thread it: fresh arm writes it into the header; resume arm reads it back from the validated header. `FlowSetup` (case class in FlowLifecycle, has `store, featureBranch, startBranch, stackSettings, branchMode, untrackedOnFailure`) gains the commit; `BranchBinding` is the natural intermediate carrier. `abortIfNoCommits` guarantees `headCommit()` is `Some` on a fresh run.\n\n### Context threading\n`FlowContext` trait (`flow/src/main/scala/orca/FlowContext.scala`) gains `private[orca] def startingCommit: Option[String]` — NOTE: the spec writes `String`, but the missing/invalid-header case must be representable (final review skips, run continues), so `Option[String]` is the right shape; `None` = skip. Consumed only by the WholeRun resolution at `reviewAndFixLoop` entry (emit warning `OrcaEvent.Step` + return `IgnoredIssues(Nil)` before constructing the loop when `None`). Implementors: `DefaultFlowContext` (`runner/src/main/scala/orca/runner/DefaultFlowContext.scala`, plain `val` constructor fact) wired at `runner/src/main/scala/orca/flow.scala:442-461` from `flowSetup` (pattern: `stackSettings = flowSetup.stackSettings`); test doubles `TestFlowContext` + `TestFlowControl` (`flow/src/test/scala/orca/TestFlowContext.scala`; `TestFlowControl.create` seeds a real `GitRepo.seeded()` repo + writes a header — seed `startingCommit` from the repo's head so WholeRun tests work).\n\n### reviewThenFix — target signature (export from `runner/src/main/scala/orca/exports.scala`, `orca.review` block)\n`def reviewThenFix[B <: BackendTag](coderSession: FlowSession[B], reviewers: List[Agent[?]], task: Task, userRequest: Option[String] = None, formatCommands: Configured[List[String]] = Configured.FromSettings, lint: Configured[Lint] = Configured.FromSettings)(using FlowContext, InStage, FlowControl, WorkspaceWrite): IgnoredIssues`. Deliberately narrower than the loop: no maxIterations/reviewerSelection/fixInstructions/diff — selection is agentDriven, diff is the stage sample; consumers are version-pinned scripts, add params only when a caller needs them. Semantics: format → select once → fan out reviewers + lint once → if findings, ONE fix turn → reconcile → announce → return; `ignored` + `unaccounted` come back in `IgnoredIssues` (nothing silently dropped), fixed claims trusted unverified. Implement via the round-primitive refactor of `ReviewFixLoop` (one `run()` loop + one `runOnce()`), NOT a third skeleton — this also discharges the drift noted in `docs/research/2026-08-review/review-loop/06-dual-loop-skeleton-drift.md`.\n\n### Tests infrastructure\n`flow/src/test/scala/orca/review/ReviewLoopFixture.scala`: `StepCapture` (collects Step messages), `coderSession(agent)` (FlowSession via private ctor), `control(dispatcher, lead, ...)` (TestFlowControl over a real temp git repo — needed for the fix turn's progress store and for `ReviewerSelector.default`'s `ctx.reviewAgent.cheap`), `FakeAgent(name, outputs)` (scripted replies; a call past the end throws — how tests pin \"must not run\"), `issue(desc, severity)`, `selector(narrow)`, `titled(title)`. Existing loop tests: `ReviewAndFixTest`, `ReviewFixFlowTest`, `FixLoopTest` — the round-primitive refactor must keep them green. Git-level tests use `orca.testkit.GitRepo`. Lifecycle tests: `runner/src/test/scala/orca/runner/FlowLifecycleTest.scala`; progress tests: `flow/src/test/scala/orca/progress/` (ProgressLogTest, ProgressStoreTest, RecoveryCheckTest).\n\n### Flow adoption facts\nAll five flows currently call `reviewAndFixLoop(coderSession = session, reviewers = allReviewers(reviewAgent), task = task, [userRequest = Some(issuePayload),] maxIterations = 3)` inside `stage(s\"Task: ...\")`. `stage[T: JsonData](name)(body: (InStage, WorkspaceWrite) ?=> T)(using FlowControl): T` — `IgnoredIssues` already has JsonData (it's a stage result today). Final review: `stage(\"Final review\"): reviewAndFixLoop(..., task = Task(Title(<final-review title>), plan.brief), diff = ReviewDiff.WholeRun, maxIterations = 5)` — the literal `5`, no public constant. Placement: implement/implement-interactive — after the task loop; implement-enhanced — AFTER \"Update documentation\", before `openPrFromBranch`; issue-pr — before `openPrFromBranch`, `userRequest = Some(issuePayload)`; issue-pr-bugfix — per-fix-task `reviewThenFix` is inside the `planAndImplementFix` helper (its `fixPlan` is local to the helper, so the final-review stage after it must build its own `Task` — e.g. from the issue payload — or the stage moves to the helper's end; final review goes before \"Push fix + finalise PR\"; the branch still carrying the failing test is fine, fix tasks are done by then). `simple.sc` keeps its `reviewAndFixLoop(..., maxIterations = 3)` — it's the DefaultMaxIterations pin. `review.sc` unchanged. `BuiltInFlowsTest.scala:36` (\"every fix-loop call states the library's default cap\") is regex-based over the flow resources; rework it so: `reviewThenFix` calls carry no cap, final-review `reviewAndFixLoop` calls state `maxIterations = 5` (hardcoded in the test with a comment), `simple.sc`'s loop still states `DefaultMaxIterations`.\n\n### Docs + ADR\nREADME: quick-start example mirrors `implement.sc` (around line 116) and must show the new shape; \"Review utilities\" table at line ~639 needs a `reviewThenFix` row, `ReviewDiff.WholeRun`, and the final-review pattern. Keep the reference tables intact — cut repeated prose, never the reference. ADR 0022 (`adr/0022-review-rework.md`) already records this PR (§2); amend only on genuine deviation (the `Option[String]` context shape does not rise to that — the ADR doesn't specify the type).\n\n### Gotchas\n- Live sanity run: `examples/runnable/01-simple/create-test-project.sh` seeds the calculator example; if all commands suddenly exit 1 during live runs, /tmp is full — clean `/tmp/orca-*` first.\n- The final-review warning path must name the next action (AGENTS.md: every refusal names the next action) — say the final review is skipped and why.\n- Don't cite plan/finding labels in comments; cite ADR 0022 or state facts inline."}},{"id":"Task: Refactor ReviewFixLoop into named round primitives (behavior-preserving)#0","name":"Task: Refactor ReviewFixLoop into named round primitives (behavior-preserving)","resultJson":{"issues":[{"title":"fixTurn bakes in a loop-continuation announcement, so a single-round caller will print a false \"reviewing again\" line","reason":"below the Info confidence gate (0.55 < 0.8)"},{"title":"`ctx`-explicit rationale restated in full, breaking the file's back-reference pattern","reason":"below the Info confidence gate (0.5 < 0.8)"},{"title":"Both new doc comments end with a clause justifying the refactor rather than describing the method","reason":"below the Info confidence gate (0.55 < 0.8)"},{"title":"Selection preparation stays inline in run(), so a single round still isn't callable on its own","reason":"below the Info confidence gate (0.6 < 0.8)"},{"title":"fixTurn is class-private, so fixLoop keeps its own copy of the same reconcile+announce step","reason":"below the Info confidence gate (0.45 < 0.8)"},{"title":"New scaladoc restates the file header and rationale already stated twice nearby","reason":"below the Info confidence gate (0.55 < 0.8)"},{"title":"New scaladoc claims a purity the round primitives don't have","reason":"below the Info confidence gate (0.45 < 0.8)"}]}}],"sessions":[{"name":"implementer","occurrence":0,"id":"e7a5b4d9-1097-4a94-af96-306dc3d4c5cc","seed":"## Codebase briefing — read before implementing any task\n\nPer CLAUDE.md, invoke the `direct-style-scala` skill before writing/reviewing any Scala here. Build/test commands are in CONTRIBUTING.md; use plain `sbt` (not `sbt --client`). Zero compiler warnings, scalafmt (`flows/*.sc` are OUTSIDE scalafmt's scope). Conventions (AGENTS.md): braceless syntax, explicit return types on public members, no default values on domain/persisted fields (one sanctioned exception below), comments state present-tense facts only (never change history), tests target exactly one scenario, named args for same-typed adjacent params.\n\n### Modules\n- `flow/` — review loop (`orca.review`), progress log (`orca.progress`), `FlowContext`/`FlowControl`, `stage`. Tests in `flow/src/test/scala/orca/`.\n- `runner/` — `flow()` entry (`runner/src/main/scala/orca/flow.scala`), `FlowLifecycle`, `DefaultFlowContext`, `exports.scala` (public re-export surface).\n- `shell/` — packages `flows/*.sc` as resources; `shell/src/test/scala/orca/shell/flows/BuiltInFlowsTest.scala` pins them.\n- `flows/*.sc` — the shipped scripts; version-pinned consumers (`//> using dep \"org.virtuslab::orca:0.1.4\"`; the shell's dev-version extraction rewrites the pin, so local testing works via `sbt publishLocal`).\n\n### ReviewLoop.scala (`flow/src/main/scala/orca/review/ReviewLoop.scala`)\nCompiled under `captureChecking`/`separationChecking` (file-header imports). Consequences you must preserve: `fc: FlowControl`/`ws: WorkspaceWrite` are METHOD parameters on `ReviewFixLoop`, never fields; the reviewer fan-out goes through `CheckedPar.mapParUnordered` and the explicit `.++[() => AgentOutcome]` type application is CC-forced (deleting it breaks the compile); the selector's `List[ReviewBatch] -> List[RosterEntry]` uses the pure-function `->` arrow; tapir-`derives` types cannot live in this file (see FixRequest.scala). `orca.CcNegativeCompileTest` pins the fan-out enforcement.\n\nCurrent shape (post PR 2, simpler than the spec's snapshot — no gate machinery exists): `reviewAndFixLoop[B <: BackendTag](coderSession: FlowSession[B], reviewers: List[Agent[?]], task: Task, userRequest: Option[String] = None, reviewerSelection: ReviewerSelector = ReviewerSelector.default, formatCommands: Configured[List[String]] = Configured.FromSettings, lint: Configured[Lint] = Configured.FromSettings, maxIterations: Int = DefaultMaxIterations, fixInstructions: String = ReviewLoopPrompts.Fix, diff: ReviewDiff = ReviewDiff.SampleFromStage)(using ctx: FlowContext, ev: InStage, fc: FlowControl, ws: WorkspaceWrite): IgnoredIssues`. It resolves `Configured` values and the `ReviewDiffSource` at entry (reads `fc.stageBaseCommit` there so the loop carries plain data), then constructs `private[review] class ReviewFixLoop(config: ReviewLoopConfig)(using ctx, ev)` and calls `run()(using fc, ws)`. Inside: `evaluate(state, selectRound, declined)` = `formatWorkspace()` + selector narrowing + `runReviewersAndLint` (the CheckedPar fan-out, per-agent `OrcaEvent.Step`s); `fix(issues)` = `coderSession.resultAs[FixOutcome].run(FixRequest(...))`; `run()` does two-phase selection (`reviewerSelection.prepare(roster, task.title, diffSource.selectorFiles)` ONCE at loop start — the agentDriven picker's LLM call happens here) then a tailrec loop applying `stopPolicy` per round. Shared helpers to reuse in the single-pass path: `stopPolicy`, `recordIgnored` (title-keyed merge), `carryPastFixes`, `announceExit`, `announceFixTurn`, `fixerHaltAdditions` (`NoFixesReason`), `indexSeverities`, `cleanExitMessage`/`FixerHaltMessage`. `ctx`/`ev` are passed EXPLICITLY at several call sites (constructor, announce calls, `prepare`) because the more-specific `fc: FlowControl` would otherwise win implicit search — keep that pattern. `DefaultMaxIterations: Int = 3` stays and keeps its flow-pin role. `ReviewerSelector.agentDriven` (parameterless, resolves `ctx.reviewAgent.cheap`) is the single-round selector for `reviewThenFix`; `ReviewerSelector.default` = agentDriven + narrowing across rounds.\n\n### ReviewDiffSource.scala (`flow/src/main/scala/orca/review/ReviewDiffSource.scala`)\n`enum ReviewDiff { SampleFromStage, Pinned(diff) }` (public) resolves to `private[review] sealed trait ReviewDiffSource` with `sample(): DiffSample`, `base: Option[String]`, `diffIntro: String`, `selectorFiles: List[String]`. `Sampled(git: GitTool, base: Option[String])` already does exactly what WholeRun needs mechanically: `git.reviewChanges(base)` (working tree vs any rev, untracked included, re-sampled per call) bounded via `BoundedDiff.reviewPayload`, `selectorFiles = git.changedFiles(base)`. Only its `diffIntro` wording (\"everything this task has changed since its stage began\") is per-stage-specific — WholeRun needs its own intro stating the change set covers everything since the run started. New-case-vs-parameterised-Sampled is implementer's choice. `GitTool` needs NO changes: `headCommit(): Option[String]` (full hash), `reviewChanges(since: Option[String]): ReviewSample`, `changedFiles(since: Option[String]): List[String]` all exist (`tools/src/main/scala/orca/tools/GitTool.scala`).\n\n### Progress header + lifecycle\n`ProgressHeader` (`flow/src/main/scala/orca/progress/ProgressLog.scala`): `startingBranch, branch, promptHash, branchMode, userPrompt: Option[String] = None, flowName: Option[String] = None`. The `Option = None` shape on the last two is the SANCTIONED exception to the no-defaults rule (ProgressLog tolerant decoding, documented at the codec) — `startingCommit: Option[String] = None` follows that precedent, so the many existing `ProgressHeader(...)` construction sites in tests keep compiling; fresh runs always write `Some`. The header is untrusted on load: `RecoveryCheck.validateHeader` (`flow/src/main/scala/orca/progress/RecoveryCheck.scala`) hard-aborts on bad branch/hash — but `startingCommit` must be LENIENT (missing OR non-hex ⇒ treated as absent; the final review later skips with a warning Step, tests pin \"missing/invalid skips with a warning\"). Validate as a hex string only — it's only ever a read-only diff base (arg-injection safety).\n\n`FlowLifecycle` (`runner/src/main/scala/orca/runner/FlowLifecycle.scala`): `setup(...)` runs cleanliness policy, `resolveStackSettings`, then `session.bindBranch(startBranch, protectedBranches, discovered)` which routes fresh (`freshBinding` → `freshRun`, which creates the branch, optionally commits discovered settings, then `store.writeHeader(ProgressHeader(...))` + `git.forceCommitOnly`) vs resume (`resumeBinding`, which validates the header). Capture `git.headCommit()` at the binding point — after the cleanliness stash, BEFORE `freshRun`'s settings/header commits — and thread it: fresh arm writes it into the header; resume arm reads it back from the validated header. `FlowSetup` (case class in FlowLifecycle, has `store, featureBranch, startBranch, stackSettings, branchMode, untrackedOnFailure`) gains the commit; `BranchBinding` is the natural intermediate carrier. `abortIfNoCommits` guarantees `headCommit()` is `Some` on a fresh run.\n\n### Context threading\n`FlowContext` trait (`flow/src/main/scala/orca/FlowContext.scala`) gains `private[orca] def startingCommit: Option[String]` — NOTE: the spec writes `String`, but the missing/invalid-header case must be representable (final review skips, run continues), so `Option[String]` is the right shape; `None` = skip. Consumed only by the WholeRun resolution at `reviewAndFixLoop` entry (emit warning `OrcaEvent.Step` + return `IgnoredIssues(Nil)` before constructing the loop when `None`). Implementors: `DefaultFlowContext` (`runner/src/main/scala/orca/runner/DefaultFlowContext.scala`, plain `val` constructor fact) wired at `runner/src/main/scala/orca/flow.scala:442-461` from `flowSetup` (pattern: `stackSettings = flowSetup.stackSettings`); test doubles `TestFlowContext` + `TestFlowControl` (`flow/src/test/scala/orca/TestFlowContext.scala`; `TestFlowControl.create` seeds a real `GitRepo.seeded()` repo + writes a header — seed `startingCommit` from the repo's head so WholeRun tests work).\n\n### reviewThenFix — target signature (export from `runner/src/main/scala/orca/exports.scala`, `orca.review` block)\n`def reviewThenFix[B <: BackendTag](coderSession: FlowSession[B], reviewers: List[Agent[?]], task: Task, userRequest: Option[String] = None, formatCommands: Configured[List[String]] = Configured.FromSettings, lint: Configured[Lint] = Configured.FromSettings)(using FlowContext, InStage, FlowControl, WorkspaceWrite): IgnoredIssues`. Deliberately narrower than the loop: no maxIterations/reviewerSelection/fixInstructions/diff — selection is agentDriven, diff is the stage sample; consumers are version-pinned scripts, add params only when a caller needs them. Semantics: format → select once → fan out reviewers + lint once → if findings, ONE fix turn → reconcile → announce → return; `ignored` + `unaccounted` come back in `IgnoredIssues` (nothing silently dropped), fixed claims trusted unverified. Implement via the round-primitive refactor of `ReviewFixLoop` (one `run()` loop + one `runOnce()`), NOT a third skeleton — this also discharges the drift noted in `docs/research/2026-08-review/review-loop/06-dual-loop-skeleton-drift.md`.\n\n### Tests infrastructure\n`flow/src/test/scala/orca/review/ReviewLoopFixture.scala`: `StepCapture` (collects Step messages), `coderSession(agent)` (FlowSession via private ctor), `control(dispatcher, lead, ...)` (TestFlowControl over a real temp git repo — needed for the fix turn's progress store and for `ReviewerSelector.default`'s `ctx.reviewAgent.cheap`), `FakeAgent(name, outputs)` (scripted replies; a call past the end throws — how tests pin \"must not run\"), `issue(desc, severity)`, `selector(narrow)`, `titled(title)`. Existing loop tests: `ReviewAndFixTest`, `ReviewFixFlowTest`, `FixLoopTest` — the round-primitive refactor must keep them green. Git-level tests use `orca.testkit.GitRepo`. Lifecycle tests: `runner/src/test/scala/orca/runner/FlowLifecycleTest.scala`; progress tests: `flow/src/test/scala/orca/progress/` (ProgressLogTest, ProgressStoreTest, RecoveryCheckTest).\n\n### Flow adoption facts\nAll five flows currently call `reviewAndFixLoop(coderSession = session, reviewers = allReviewers(reviewAgent), task = task, [userRequest = Some(issuePayload),] maxIterations = 3)` inside `stage(s\"Task: ...\")`. `stage[T: JsonData](name)(body: (InStage, WorkspaceWrite) ?=> T)(using FlowControl): T` — `IgnoredIssues` already has JsonData (it's a stage result today). Final review: `stage(\"Final review\"): reviewAndFixLoop(..., task = Task(Title(<final-review title>), plan.brief), diff = ReviewDiff.WholeRun, maxIterations = 5)` — the literal `5`, no public constant. Placement: implement/implement-interactive — after the task loop; implement-enhanced — AFTER \"Update documentation\", before `openPrFromBranch`; issue-pr — before `openPrFromBranch`, `userRequest = Some(issuePayload)`; issue-pr-bugfix — per-fix-task `reviewThenFix` is inside the `planAndImplementFix` helper (its `fixPlan` is local to the helper, so the final-review stage after it must build its own `Task` — e.g. from the issue payload — or the stage moves to the helper's end; final review goes before \"Push fix + finalise PR\"; the branch still carrying the failing test is fine, fix tasks are done by then). `simple.sc` keeps its `reviewAndFixLoop(..., maxIterations = 3)` — it's the DefaultMaxIterations pin. `review.sc` unchanged. `BuiltInFlowsTest.scala:36` (\"every fix-loop call states the library's default cap\") is regex-based over the flow resources; rework it so: `reviewThenFix` calls carry no cap, final-review `reviewAndFixLoop` calls state `maxIterations = 5` (hardcoded in the test with a comment), `simple.sc`'s loop still states `DefaultMaxIterations`.\n\n### Docs + ADR\nREADME: quick-start example mirrors `implement.sc` (around line 116) and must show the new shape; \"Review utilities\" table at line ~639 needs a `reviewThenFix` row, `ReviewDiff.WholeRun`, and the final-review pattern. Keep the reference tables intact — cut repeated prose, never the reference. ADR 0022 (`adr/0022-review-rework.md`) already records this PR (§2); amend only on genuine deviation (the `Option[String]` context shape does not rise to that — the ADR doesn't specify the type).\n\n### Gotchas\n- Live sanity run: `examples/runnable/01-simple/create-test-project.sh` seeds the calculator example; if all commands suddenly exit 1 during live runs, /tmp is full — clean `/tmp/orca-*` first.\n- The final-review warning path must name the next action (AGENTS.md: every refusal names the next action) — say the final review is skipped and why.\n- Don't cite plan/finding labels in comments; cite ADR 0022 or state facts inline.","resumeWireId":"e7a5b4d9-1097-4a94-af96-306dc3d4c5cc","backend":"ClaudeCode"}]}