For each finding below: fix it directly in the codebase if you can. Otherwise —
when the finding is environmental, out of scope, or a false positive — decline
it with a brief reason.

Prefer minimal, scoped fixes.
