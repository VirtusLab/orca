Summarise the lint output into a ReviewResult. Each distinct problem should
produce a ReviewFinding. Report only the findings worth fixing — judge that on
the evidence in the output, not on how strongly the tool phrased it.
